#!/usr/bin/env python3

import rospy
import sys
from std_msgs.msg import String
import cv_bridge import

def callback(msg):
    cv::Mat frame = cv::Mat frame = cv_bridge::toCvShare(msg, "bgr8")->image;
(msg, "bgr8")->image;

def listener():
    rospy.init_node('mike_inference', anonymous=True)
    rospy.Subscriber('mike_camera/raw', String, callback)
    
    rospy.spin()

if __name__ == '__main__':
    listener()
